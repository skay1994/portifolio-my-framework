on:
  push:
    branches-ignore:
      - master

permissions:
  contents: read

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup PHP Environment
        uses: nanasess/setup-php@v4
        with:
          php-version: '8.3'
